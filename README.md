# Simple Library
My project for Yandex Lyceum

#

To install all required Python libraries, run:
`pip install -r requirements.txt` in project folder.

The main Python file is "**app.py**"

#

Чтобы установить все необходимые библиотеки Python, введите: 
`pip install -r requirements.txt` в папке проекта.

Главный Python файл - "**app.py**"
